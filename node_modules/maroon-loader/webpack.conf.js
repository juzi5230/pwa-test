const path = require("path");

module.exports = {
  entry: './app/entry.js',
  output: {
    path: path.join(__dirname, "dist"),
    filename: 'dist.js',
    publicPath: 'dist/'
  },
  module: {
    noParse: /lodash|maroon-loader/,
    rules: [{
      test: /\.js$/,
      exclude: /(node_modules)/,
      use: [{
        loader: 'babel-loader',
        options: {
          presets: [['es2015', {modules: false}]],
          plugins: ['syntax-dynamic-import']
        }
      }]
    }]
  }
};
